/**
 * TipTap JSON for a brand-new narrative (StarterKit empty doc).
 * Keep in sync with what `editor.getJSON()` produces for an empty editor.
 */
export const EMPTY_NARRATIVE_CONTENT: Record<string, unknown> = {
  type: 'doc',
  content: [{ type: 'paragraph' }]
}
